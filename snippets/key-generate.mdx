<CodeGroup>

```bash openssl
umask 077
# genpkey writes PKCS#8, the one format all three clients load.
openssl genpkey -algorithm EC -pkeyopt ec_paramgen_curve:P-256 -out "$HEVN_KEY_PEM"
# The only thing you copy anywhere: the base64 DER public key.
openssl ec -in "$HEVN_KEY_PEM" -pubout -outform DER 2>/dev/null | base64 | tr -d '\n'
```

```python Python
import base64, os
from cryptography.hazmat.primitives import serialization
from cryptography.hazmat.primitives.asymmetric import ec

key = ec.generate_private_key(ec.SECP256R1())
pem = key.private_bytes(
    serialization.Encoding.PEM,
    serialization.PrivateFormat.PKCS8,
    serialization.NoEncryption(),
)
path = os.environ["HEVN_KEY_PEM"]
with open(os.open(path, os.O_CREAT | os.O_WRONLY | os.O_TRUNC, 0o600), "wb") as handle:
    handle.write(pem)

spki = key.public_key().public_bytes(
    serialization.Encoding.DER,
    serialization.PublicFormat.SubjectPublicKeyInfo,
)
print(base64.b64encode(spki).decode())
```

```javascript Node
import { generateKeyPairSync } from "node:crypto";
import { writeFileSync } from "node:fs";

const { privateKey, publicKey } = generateKeyPairSync("ec", { namedCurve: "prime256v1" });

writeFileSync(
  process.env.HEVN_KEY_PEM,
  privateKey.export({ type: "pkcs8", format: "pem" }),
  { mode: 0o600 },
);

console.log(publicKey.export({ type: "spki", format: "der" }).toString("base64"));
```

```go Go
key, err := ecdsa.GenerateKey(elliptic.P256(), rand.Reader)
if err != nil {
	return err
}
pkcs8, err := x509.MarshalPKCS8PrivateKey(key)
if err != nil {
	return err
}
armored := pem.EncodeToMemory(&pem.Block{Type: "PRIVATE KEY", Bytes: pkcs8})
if err := os.WriteFile(os.Getenv("HEVN_KEY_PEM"), armored, 0o600); err != nil {
	return err
}
spki, err := x509.MarshalPKIXPublicKey(&key.PublicKey)
if err != nil {
	return err
}
fmt.Println(base64.StdEncoding.EncodeToString(spki))
```

</CodeGroup>
