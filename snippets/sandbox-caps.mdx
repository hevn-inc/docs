<Note>
  The sandbox is the same API against one emulated partner on Base Sepolia. Its money comes from a shared treasury: at most **100 tokens per credit**, at least `0.10`, and **60 money calls per account per hour**. `GET /v1/sandbox/treasury` reports the live ceiling. Full table in [Limits](/whitelabel/reference/limits#sandbox).
</Note>
