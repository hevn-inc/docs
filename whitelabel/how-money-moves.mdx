---
title: "How money moves"
description: "Where a client balance lives, which legs are partner conversions, and what is never instant."
---

Every client balance is a stablecoin on one address. Everything else is a partner moving fiat to or
from it.

```mermaid
flowchart LR
    W["Client's Base smart wallet<br/>USDC / EURC"]
    subgraph In["Fiat in"]
        P["Payer's bank"] --> R["Requisites in the client's<br/>legal name — POST /v1/banks"]
    end
    subgraph Crypto["Crypto in"]
        S["Any sender on Base"]
    end
    subgraph Out["Fiat out"]
        D["debit.address<br/>HEVN treasury"] --> RB["Recipient's bank"]
    end
    R -->|"partner converts — POST /v1/payins"| W
    S -->|"USDC or EURC"| W
    W -->|"POST /v1/payouts then /confirm"| D
    B["GET /v1/clients/…/balance"] -.->|"reads this address"| W
```

## A balance is a stablecoin on one address

`POST /v1/clients` deploys a Base smart wallet for the client and returns its address as
`baseSmartWallet`. That address is the balance: `GET /v1/clients/{clientId}/balance` reads it on
chain, one row per settlement token, and no HEVN database holds an authoritative copy.

Two tokens can sit there: **USDC** and **EURC**. Which one a rail settles into is the rail's
`settlementToken`, chosen from its `supportedTokens` and switched with
`PUT /v1/banks/{rail}/settlement-token`.

## Fiat in is a conversion you priced

A rail is a bank route you open for one client. Opening it publishes **requisites** — an IBAN, an
account and routing number, a PIX key — in the client's own legal name. Money wired there is
received by the partner bank, converted at the rate the rail quotes, and delivered to the client's
wallet as the rail's settlement token, minus the rail's `feeBps`.

You can price that arrival before it happens. `POST /v1/payins` quotes an incoming amount and
`POST /v1/payins/{payinId}/instructions` returns what the payer needs to see. Money that arrives
without a quote — an unsolicited wire to open requisites — still lands and still becomes an income
row; the quote exists so you can show your customer a number first.

Crypto in needs no rail and no quote. Anything sent to `baseSmartWallet` on Base is the client's
balance the moment it confirms.

## Fiat out is funded from the wallet

`POST /v1/payouts` does the whole opening move: it prices the payment, books it with the partner, and
builds the on-chain operation that funds it. The response carries three things you act on — the
`quote` (what the recipient gets and what it costs), the `approval` (the bytes to sign) and the
`debit` (exactly what leaves the client's wallet: an amount of USDC to HEVN's treasury address).
`POST /v1/payouts/{payoutId}/confirm` with your signature executes that debit, and the partner pays
the recipient's bank.

The side you typed is inviolable. Send `amount` to pin what leaves the balance, or `amountTo` to pin
what the recipient receives — never both, and never re-derive the other side yourself.

A **transfer** is the same envelope with no fiat leg: `POST /v1/transfers` then
`POST /v1/transfers/{transferId}/confirm` sends USDC from the client's wallet to an address you saved
as a contact.

## Progress is read, not pushed

Only the chain leg is fast, and even it is not synchronous: a confirm answers `200` with a
`transactionHash` when the receipt lands inside the request, and `202 submitted` when it does not —
in which case you confirm again, which is always safe. Every fiat leg is slower than that by a
partner's business hours, so the state you act on comes from a read:
`GET /v1/payouts/{payoutId}`, `GET /v1/payins/{payinId}`, `GET /v1/banks`,
`GET /v1/clients/{clientId}/balance`.

## One ledger per client

Both directions end in the same place: a transaction row on the client.
`GET /v1/transactions` with `X-Hevn-Account: cl_…` walks it newest first,
`GET /v1/transactions/summary` totals the same filters, and `GET /v1/transactions/export` renders a
statement. That ledger, not your own bookkeeping, is what a client's customer support answers from.

<Note>
  Every amount on the wire is a decimal string in the asset's major unit — `"500.00"`, never `500`
  and never `"500000000"`. See [Conventions](/whitelabel/conventions#money).
</Note>

<Card title="Next: Quickstart" icon="rocket" href="/whitelabel/quickstart">
  Do all of this once, in the sandbox, in about twenty minutes.
</Card>
